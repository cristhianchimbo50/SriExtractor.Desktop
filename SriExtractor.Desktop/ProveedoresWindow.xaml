<Window x:Class="SriExtractor.Desktop.ProveedoresWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proveedores"
        Height="520"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource PageBackground}">

    <Window.Resources>
        <SolidColorBrush x:Key="PageBackground" Color="#FFF4F5F7"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="BorderBrushLight" Color="#FFCED4DA"/>
    </Window.Resources>

    <Grid Margin="16" Background="{StaticResource PageBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Cargar proveedores"
                        Width="160"
                        Height="36"
                        Click="BtnLoad_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Padding="10" Margin="0,12,0,12">
            <DataGrid x:Name="GridProveedores"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserResizeColumns="False"
                      CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="RUC/CI"
                                        Binding="{Binding RucCi}"
                                        Width="160"/>
                    <DataGridTextColumn Header="Razón social"
                                        Binding="{Binding RazonSocial}"
                                        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="2" Padding="10">
            <TextBlock x:Name="LblStatus"
                       Text="Listo."
                       TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>
